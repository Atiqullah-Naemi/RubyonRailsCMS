<%= form_for [:admin, @tag] do |f| %>
	<div class="form-group">
		<%= f.text_field :name, class: 'form-control', placeholder: 'Ex. Ruby, Java', autofocus: true %>
	</div>
	<div class="form-group">
		<%= f.submit class: 'btn btn-default pull-right' %>
	</div>
<% end %>